(() => { let e = document.getElementById("api-users-nodeJS-btn"), t = document.getElementById("api-users-PHP-btn"), n = document.querySelector(".GET-time-result"), o = document.querySelector(".api-users-reqs"), l = 1; o.addEventListener("keyup", (e => { l = Number(e.target.value) })), console.log("started"), e.addEventListener("click", (async e => { if (isNaN(l) || 0 == l) return !1; { let e = (new Date).getTime(); for (let e = 0; e < l; e++)await fetch("http://localhost:8000/api/users", { method: "GET" }).then((e => e.json())); let t = (new Date).getTime() - e; n.innerHTML = `${t} мс`, console.log(t) } })), t.addEventListener("click", (async e => { if (isNaN(l) || 0 == l) return !1; { let e = (new Date).getTime(); for (let e = 0; e < l; e++)await fetch("http://localhost:4000/api/users", { method: "GET" }).then((e => e.json())); let t = (new Date).getTime() - e; n.innerHTML = `${t} мс`, console.log(t) } })) })();